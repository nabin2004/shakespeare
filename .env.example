# Copy to .env and fill in. Never commit .env.

# Phase 2+: OpenAI (or compatible) API for NL-to-SPARQL translation and narration
OPENAI_API_KEY=

# Oxigraph SPARQL endpoint (default matches docker compose internal URL)
OXIGRAPH_URL=http://localhost:7878/query

# Phase 2+: optional API auth for write/proxy routes
# API_SECRET_KEY=
